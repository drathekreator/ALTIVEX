{
  "name": "altivex-frontend-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Test harness untuk frontend ALTIVEX (exploration & preservation PBT). Bukan paket runtime; index.html tetap berdiri sendiri sebagai static file yang di-serve actix-files.",
  "scripts": {
    "test": "vitest run",
    "test:exploration": "vitest run tests/exploration.spec.js"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0",
    "fast-check": "^3.19.0",
    "@vitest/ui": "^1.6.0"
  }
}
